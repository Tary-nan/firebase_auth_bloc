
import 'package:flutter/material.dart';

class SplashScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print('splash');
    return Scaffold(
      body: Center(child: Text("Splash screen page")),
    );
  }
}